用户的注册
    1. 提供一个静态的html页面，表单让用户注册信息
    2. 提交表单到后台，后台对提交的数据进行验证，是否允许注册，如果注册成功
    则返回成功的页面，如果失败则返回失败的页面
    3. 把用户注册的信息存储在一个文件中

用户的登录
    1. 提供一个静态的页面，表单让用户登录
    2. 提交登录信息后，后台有专门负责是否允许登录的处理
    3. 登录成功则显示成功的页面，失败则显示失败的页面